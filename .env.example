# Application runtime configuration for Code Adventurers
# Copy this file to .env (or export the variables) before starting the API service.

# Node environment (development|test|production)
NODE_ENV=development

# Port used when you bootstrap the Express server manually
PORT=3000

# --- MySQL connection ---
# Preferred single URL format supported by the service layer
MYSQL_URL=mysql://root:secret@127.0.0.1:3306/code_adventurers

# Optional prefix automatically prepended to every table name (useful for shared databases)
MYSQL_TABLE_PREFIX=

# --- Legacy split variables ---
# If you prefer to manage connection parts individually, fill the values below and leave MYSQL_URL blank.
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=secret
MYSQL_DATABASE=code_adventurers
